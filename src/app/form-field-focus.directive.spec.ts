import { FormFieldFocusDirective } from './form-field-focus.directive';

describe('FormFieldFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldFocusDirective();
    expect(directive).toBeTruthy();
  });
});
